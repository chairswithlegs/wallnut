if posts
    ul.list-group
        each post in posts
            li.list-group-item
                a.post.title
else
    p You haven't created any posts.

button.btn.btn-primary(type='button') Create Post